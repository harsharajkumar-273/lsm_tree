name: LSM-Tree CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout codebase
      uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake liburing-dev pkg-config

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=Release

    - name: Build project targets
      run: cmake --build build --config Release

    - name: Run Unit Tests & Compaction Coverage
      run: |
        ./build/test_skip_list
        ./build/test_bloom_filter
        ./build/test_wal_recovery
        ./build/test_sstable
        ./build/test_compaction_streaming
        ./build/test_block_cache

    - name: Run Benchmarks & Regression Detection
      run: |
        ./build/bench_write
        ./build/bench_read
        ./build/bench_block_bottleneck
